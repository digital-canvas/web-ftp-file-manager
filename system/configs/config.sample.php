<?php
// Displayed in the header for all pages
$config['app']['name'] = 'Web FTP Manager';

// The ftp server hostname that the application connects to
$config['ftp']['server'] = 'localhost';

// This is the displayed value for the server hostname.
// This might be different that the actual hostname that is connected to
// For example the connection might need to be made with an ip address but you want to display a domain to the user.
$config['ftp']['name'] = 'localhost';

